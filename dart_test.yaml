# Screenshot rendering is run on demand, not with the regular suite:
#   flutter test --update-goldens --tags screenshots test/screenshots
tags:
  screenshots:
    skip: "Run on demand with --tags screenshots"
