# Screenshot rendering is run on demand, not with the regular suite:
#   flutter test --update-goldens --run-skipped --tags screenshots test/screenshots
tags:
  screenshots:
    skip: "Run on demand with --run-skipped --tags screenshots"
