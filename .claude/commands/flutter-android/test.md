---
description: Run Flutter tests with a configurable concurrency limit.
allowed-tools:
  - Bash
argument-hint: "[--jobs N] [test-path] [--dry-run]"
---

Run from the Flutter project root:

```sh
sh .claude/ctk/modules/flutter-android/scripts/test.sh $ARGUMENTS
```
