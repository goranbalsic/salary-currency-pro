---
description: Run release preflight, build an App Bundle, and optionally build APKs.
allowed-tools:
  - Bash
argument-hint: "[--apk] [--split-per-abi] [--flavor NAME] [--target PATH] [--base REF] [--jobs N] [--dry-run]"
---

Run from the Flutter project root:

```sh
sh .claude/ctk/modules/flutter-android/scripts/release.sh $ARGUMENTS
```
