# Flutter's own R8 rules (keeping io.flutter.* etc.) are applied automatically
# by the Flutter Gradle plugin. This file is for rules specific to this app's
# other dependencies, added only if a real release build actually needs them.
